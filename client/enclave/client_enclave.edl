enclave {

    trusted {
        /* define ECALLs here. */
        public void sample();
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print_string([in, string] unsigned char *str, int length);
    };
};
